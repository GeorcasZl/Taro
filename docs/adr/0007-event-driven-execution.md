# ADR 0007: 统一事件驱动执行模型

状态：已采纳（2026-06-24）

## 背景
原设计使用线性序列执行模型（Group 内的 ContentItem 按顺序执行）。
但 RPG 场景、漫画翻页等需要非线性的事件响应。

## 决策
引擎层统一使用 trigger/response 事件驱动模型。
对话、独白等常见操作是创作层的「语法糖」，
在引擎层自动展开为 trigger→response 绑定。
创作者默认使用语法糖编写，高级创作者可以直接编辑事件绑定。

## 后果
- 好处：统一模型覆盖所有叙事形态
- 代价：简单场景的内部表示更复杂；语法糖层需要精心设计
