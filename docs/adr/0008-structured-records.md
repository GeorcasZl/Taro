# ADR 0008: 结构化记录系统

状态：已采纳（2026-06-24）

## 背景
分支叙事需要追踪故事状态。原设计只支持简单的键值对
（boolean/number/text）。但 RPG 背包、角色属性等
需要更复杂的数据结构。

## 决策
记录系统支持结构化类型：
- 基础类型：boolean / number / text
- 复合类型：object / array
触发器可以读取记录状态（条件判断），响应可以写入记录状态。

## 后果
- 好处：能表达复杂的游戏状态
- 代价：条件编辑器和记录管理 UI 更复杂
