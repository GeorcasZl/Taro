# ADR 0009: 灵活嵌套分组

状态：已采纳（2026-06-24）

## 背景
大型叙事作品需要组织结构（章节、场景、段落）。
两层固定结构（Chapter/Scene）不够灵活，
但带执行语义的嵌套会增加引擎复杂度。

## 决策
分组是纯组织概念，像文件夹一样可以无限嵌套。
分组没有执行语义——不影响引擎行为。
场景上下文（默认背景/BGM）通过 Group 内的事件绑定实现。

## 后果
- 好处：创作者自由组织内容；引擎模型保持简洁
- 代价：「进入场景时设置背景」需要在 Group 中显式创建事件绑定
